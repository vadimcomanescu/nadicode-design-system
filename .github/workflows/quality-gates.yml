name: Quality gates

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: quality-gates-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate design system
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck design system
        run: npm run typecheck

      - name: Lint design system
        run: npm run lint

      - name: Docs drift check
        run: npm run docs:check

      - name: Nadicode contract check
        run: npm run ds:check

      - name: Nadicode AST contract check
        run: npm run ds:ast-check

      - name: Test design system with coverage
        run: npx vitest run --coverage

      - name: Build design system
        run: npm run build

      - name: Scaffold sync drift check
        run: npm run scaffold:check-sync
